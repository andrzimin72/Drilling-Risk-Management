"""
External AI Framework Adapters (AutoGen, CrewAI, LangGraph, Claude)
"""
# Adapters are usually imported directly by name to avoid loading 
# heavy dependencies (like langgraph or crewai) if they aren't installed.